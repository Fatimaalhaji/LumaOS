import Link from "next/link"; import { registerAction } from "@/server/actions";
export default function Register(){ return <main className="mx-auto grid min-h-screen max-w-md place-items-center"><form action={registerAction} className="card grid w-full gap-4"><h1 className="text-3xl font-bold">Create LumaOS account</h1><input name="name" placeholder="Name" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" minLength={8} required/><button>Register</button><Link href="/login">Already have an account?</Link></form></main>; }
