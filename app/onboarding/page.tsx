import { auth } from "@/auth"; import { redirect } from "next/navigation"; import { onboardingAction } from "@/server/actions";
export default async function Onboarding(){ const s=await auth(); if(!s?.user) redirect('/login'); return <main className="mx-auto grid min-h-screen max-w-2xl place-items-center"><form action={onboardingAction} className="card grid w-full gap-4"><h1 className="text-3xl font-bold">Set up your operating layer</h1><input name="name" defaultValue={s.user.name ?? ""} placeholder="Your name" required/><input name="primaryGoal" placeholder="Primary goal" required/><textarea name="about" placeholder="Optional: what are you working toward?"/><button>Finish onboarding</button></form></main>; }
